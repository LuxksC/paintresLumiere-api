# Optional: Bruno can load secrets from a .env file in this folder (see Bruno docs).
# This collection uses the Local environment variable `accessToken`, filled by Signup/Login scripts
# or manually in Bruno. Add keys here only if you wire them in requests/scripts.
